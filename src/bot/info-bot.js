const Discord = require('discord.js')
const config = require('../.json/config.json')

module.exports = {
    name: 'info-bot',
    execute(client, message, args){
        const embed = new Discord.MessageEmbed()
        .setDescription('Createur : ')
    }
}